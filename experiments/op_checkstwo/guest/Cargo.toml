[package]
name = "checkstwo-guest"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
checkstwo-verifier = { path = "../verifier" }
dlmalloc = { version = "=0.2.12", default-features = false }
