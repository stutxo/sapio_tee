Unreleased

- Add the Cohort Exit research harness: complete-roster equal-output batches,
  Taproot key-path settlement while depositors are offline, and independent
  owner-plus-CSV refunds. autoresearch.sh runs the real in-process ProgramOracle
  with deterministic public fixtures and measures deposit-inclusive chain
  cost. Explicitly limit privacy to a toy chain observer; no operator privacy,
  production wallet, consensus-node validation or global novelty is claimed.
  Pin the already-authorized program internal key to reduce refunds from
  138 to 130 vbytes; bound fixed-format decoding to reduce predicate WASM
  from 2,193 to 1,563 bytes. Preserve the original CE01 encoding, spending
  rules, frozen benchmark and 214.5-vbyte/user lifecycle cost.
- Fix the aarch64 enclave startup script to use the image's /bin/sh.
  writeShellScriptBin had prepended an x86_64 build-host Bash interpreter,
  preventing native ARM startup despite a successful EIF build. Rebuild the
  image and review its new PCR0/PCR2 before deployment.
- Document the distinction between compiler plugins and signing predicates,
  the custom-inline/compiled-contract workflows and the legacy CTV CLI gap.
  Verify live signing on native and emulated ARM services, including compiled
  Sapio payment contracts. Hardware Nitro/KMS validation remains outstanding.
- Add a durable, source-built safe-withdrawal vault example: inline Rust/WASM
  predicate, a real Sapio contract and a remote ProgramClient demonstration.
  Share identity/CLI checks with the existing example, reproduce the WASM in
  CI and exercise both examples against the live local signer. The vault is
  explicitly a permissionless fixed-recipient sweep, not a recovery wallet.

0.2.0 — 2026-09-18

- Build the enclave image around a minimal defconfig-based kernel (Nitro
  virtio/vsock/NSM options built in) instead of nixpkgs' all-driver common
  config, and reclaim runner disk before the EIF build: GitHub's ubuntu
  runners previously ran out of space mid-kernel-compile.

- Cache the nix store and cargo artifacts in CI: repeat pushes restore the
  kernel, odyn, and rust compilation instead of rebuilding them.

- Target aarch64: the enclave image, supervisor, and parent runner are now
  cross-compiled for Graviton Nitro Enclaves from the x86_64 build host —
  arm64 defconfig kernel (`Image`), aarch64 musl statics, aarch64 init and
  busybox. Local development remains native x86_64.

- Deploy Sapio's ProgramOracle inside x86_64 AWS Nitro Enclaves, using
  SignProgramV1, bounded inline WASM v1/v2 and a measured pay-at-least/v1
  interpreter.
- Recover the BIP32 root through attested KMS ECDH and settings-bound HKDF.
- Bind the public identity and signing profile to a fresh NSM challenge.
  Verify against independently trusted measurements, settings and profile.
- Provide offline program-profile output, pinned EIF/runner builds,
  restrictive KMS policy generation and deployment/recovery guidance.
- Include a runnable ProgramClient example demonstrating flexible payments,
  key-path and script-path signatures, inline predicates and failed requests.
- Verify signing on native and musl services, signed-document acceptance and
  rejection cases, and a complete EIF build. See VERIFICATION.txt for evidence.
- Real Nitro boot, live KMS provisioning and hardware recovery remain
  unverified; local software checks are not production-custody assurance.
