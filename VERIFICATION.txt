SOFTWARE VERIFICATION: OPTIONAL TERRAFORM VPC CREATION

Scope:
- deploy/terraform: subnet_id is now optional. Empty creates a dedicated VPC,
  subnet, Internet Gateway, route table and association; a supplied existing
  public subnet is used instead and no network resources are created. The
  instance's subnet, security-group VPC and outputs follow either branch.
- No resources were created and no apply was run. The external EIF build data
  source was not re-executed; targeting limited plans to network resources.

Commands and scenarios exercised (terraform 1.16.1, one-off nixpkgs shell):
- fmt -check -recursive and validate passed on the real configuration.
- Read-only plans against the real AWS account (-refresh=false, network
  targets only): with subnet_id empty, the plan added exactly the five network
  resources (VPC, subnet, Internet Gateway, route table, association) and
  read the region's availability zones. With a subnet_id supplied, the plan
  changed no real infrastructure and the subnet_id output resolved to the
  supplied subnet; targeting data.aws_subnet.parent with a nonexistent ID
  failed with "no matching EC2 Subnet found", proving the lookup branch.

Limitations:
- No full plan or apply was run; the instance's depends_on ordering across
  the conditional route association is statically validated, not exercised.
- The created subnet uses the region's first available AZ; m7g.xlarge
  availability is an operator region choice, as before.

SOFTWARE VERIFICATION: REVIEW-HARDENING ROUND

Scope:
- Post-review hardening of the attestation verifier, its dependencies, the
  example predicates, and the Terraform test workflow. No enclave image,
  production source, KMS policy, derivation or registry change: src/ and
  evaluators/ are untouched and EIF measurements are unaffected.
- No AWS resources were created; no Nitro hardware was used. All results are
  local software evidence on the development workstation.

Changes verified:
- scripts/verify-attestation.py: verify() itself now requires exactly the
  PCR0/1/2 pin set (no longer a CLI-caller convention), validates the settings
  shape, and base58check-decodes the verified xpub, requiring the BIP32 version
  bytes of the claimed settings network and a valid secp256k1 point. Tag-18
  wrapped COSE documents no longer fail closed on cbor2's frozendict decoding
  of the tagged unprotected header; both wrapped and bare documents verify.
- tests/test_attestation.py: 27 tests (10 prior plus 17 new), adding negative
  certificate-path fixtures (expired leaf, not-yet-valid leaf, non-CA issuer,
  expired root in the chain, P-256 leaf), future timestamp, missing nonce,
  wrong digest, ambiguous and non-ES384 COSE algorithm headers, COSE tag 17
  rejection and tag 18 acceptance, trailing CBOR, duplicate JSON fields,
  empty/partial/string-keyed pin sets, malformed/corrupt/off-curve xpubs,
  cross-network xpub claims, and a mainnet-xpub-with-mainnet-settings
  positive. All 27 pass under the Nix dev shell and under a fresh
  hash-enforced venv built from requirements-verifier.txt
  (uv pip install --require-hashes, Python 3.13).
- requirements-verifier.txt: regenerated with per-artifact SHA-256 hashes for
  the full resolved closure (cbor2 6.1.3, cffi 2.1.1, cryptography 50.0.1,
  pycparser 3.0, pyOpenSSL 26.4.0, typing-extensions 4.16.0). The previous
  pins did not resolve: pyOpenSSL 26.3.0 declares cryptography<50 while the
  file pinned cryptography==50.0.0. The hash-enforced fresh-venv install plus
  full test run above is the evidence the file installs as shipped.
- examples/vault: the predicate now requires the sweep output to reach the
  330-satoshi P2TR dust floor, so an under-proportioned deposit cannot be
  burned entirely to fees by the permissionless settler. Rebuilt vault.wasm
  with the documented recipe: 1,309 bytes, sha256
  061469c24ae73a0cbf1400d6dc57093d60a49143de1c6a0181ba7c3162c378be;
  build.sh --check reproduces it byte-identically. The previous rule accepted
  the new rejection case (by inspection of the removed condition: the in-cap
  1,000-sat fee passed and no floor existed).
- examples/support/mod.rs: Nitro identities must carry a settings network
  whose BIP32 version matches the xpub. Local-dev behavior unchanged.
- examples/vault/contract.rs: targeted clippy allowance for the upstream
  192-byte sapio::contract::CompilationError, restoring the documented clippy
  command below to a pass (the lint fired on HEAD before this round; the file
  was unchanged by it). The same allowance briefly existed in the cohort-exit
  research contract, which has since been removed from the repository.
- deploy/terraform: the network variable now refuses "bitcoin" in the
  one-apply test workflow.

Commands exercised and passing on this workstation:
- cargo fmt --all -- --check
- cargo clippy --locked --all-targets --all-features -- -D warnings
- cargo test --locked --no-default-features and --features local-dev: 4/4 each
- python3 -m unittest discover -s tests -p 'test_*.py': 27/27, run twice (Nix
  dev shell and the hash-enforced requirements venv)
- terraform 1.16.1 (one-off nixpkgs shell): fmt -check -recursive and validate
  on the real configuration; the network guard was additionally exercised in
  an isolated providerless module: "bitcoin" is refused with the future-block
  message, an unknown network is refused by the allowlist, and "regtest"
  plans successfully. No AWS-backed plan/apply was run; the external build
  data source was not re-executed.
- bash examples/vault/build.sh --check: byte-identical rebuild
- Live --local-dev ProgramClient exercises in an isolated network namespace
  (port 8000 was held by an unrelated process on this host): program_oracle
  passed all 17 scenarios and vault all 13, including the new boundary pair:
  a 200-sat sweep output from a 1,200-sat deposit with an in-cap fee is now a
  typed remote rejection, and the 330-sat dust-floor boundary signs,
  finalizes and extracts.

Limitations:
- The new verifier checks are software-tested with synthetic fixtures; no
  genuine AWS Nitro document has been parsed. The tag-18 acceptance path
  mirrors the real encoding, but hardware interop remains unverified.
- The CI workflow edit was not executed here; GitHub Actions runs it.
- Nothing in this round changes the documented hardware boundary: live Nitro
  boot, real NSM attestation, KMS policy enforcement and restart/recovery
  remain unexercised and are required before production custody.

SOFTWARE VERIFICATION: ONE-APPLY TERRAFORM TEST DEPLOYMENT

Scope:
- Extend the earlier host-only Terraform configuration into local artifact
  build, private S3 upload, attestation-bound KMS key creation, systemd launch
  and initialization, and exact-instance SSM readiness checking.
- No AWS resources were created or AWS account API calls made. Terraform AWS
  operations and SSM responses were simulated. The Nix build, policy generator,
  HTTP initializer, readiness CLI, shell execution and checksum checks were real.
- Production Rust, enclave protocol and derivation settings are unchanged.

Commands and scenarios exercised:
- Terraform 1.16.1: fmt, init -backend=false -input=false, and validate passed
  with signed hashicorp/aws 6.66.0 and hashicorp/external 2.4.2 providers.
  The provider lock is retained.
- Ran deploy/terraform/build.py through nix develop with the current repository.
  It built the real .#eif and .#enclaver with the flake lock unchanged, returning
  nonzero PCR0/PCR1/PCR2 and a 92,229,617-byte EIF. Its architecture check accepted
  the current static ARM runner and rejected the stale x86_64 result-runner.
- Four temporary mocked-provider plans passed: the complete deployment graph,
  rejection of world-open SSH, rejection of the zero recovery block hash, and
  rejection of an STS session ARN as the KMS administrator.
- A temporary mocked-provider apply passed, including real external-policy
  generation and the terraform_data local-exec provisioner. The latter actually
  called the simulated AWS CLI's describe-instance-information, send-command and
  get-command-invocation operations for the selected instance.
- Existing-key CLI and pre-CreateKey external-provider modes generated identical
  KMS policy statements. Zero PCR0, cross-account or identical admin/parent roles,
  and unexpected external query fields were rejected.
- Eleven real HTTP initializer scenarios passed: fresh setup, idempotent restart,
  concurrent setup, transient KMS failure, an 11-second setup response without a
  premature retry, mismatched settings, local-dev mode, malformed identity,
  invalid setup, persistent unhealthy status, and persistent KMS failure.
  Failure cases exited nonzero; the configured deadlines bounded retries.
- Six readiness CLI scenarios passed against simulated AWS CLI responses:
  success, delayed registration with an unrelated online instance, eventual
  command visibility, failed command, mismatched identity despite a successful
  command status, and access denial. Only the matching successful invocation
  with the expected public identity reported readiness.
- Rendered the actual bootstrap template through Terraform using hashes of the
  real artifacts. bash -n and ShellCheck 0.11.0 passed. In isolated temporary
  filesystems, bootstrap copied and hash-checked all artifacts and setup.json,
  installed the actual service unit, and ran its real ExecStartPost initializer
  against the HTTP fixture. A corrupted manifest and a failed S3 download both
  stopped before enclave-service startup or initialization.
- systemd-analyze verify passed for the actual unit in a temporary filesystem
  with stub dependency units. Package, firewall, allocator and service-control
  commands in the bootstrap smoke were simulated, not actual AL2023/Nitro work.
- nix develop --no-update-lock-file -c python3 -m unittest discover -s tests
  -p 'test_*.py': all 10 existing attestation-verifier tests passed.

Limitations:
- No live terraform apply/destroy, AL2023 cloud-init, hugepage allocation,
  Nitro launch, KMS authorization, attestation, or hardware restart/recovery
  was exercised. The protected-key destroy guard is configured, not an AWS
  deletion experiment. Mocked apply is not cloud integration evidence.
- Readiness is not cryptographic attestation. Use synthetic transactions only;
  the example regtest hash does not establish unpredictable future-block
  chronology. Verify independently before considering any custody use.

HISTORICAL VERIFICATION: MINIMAL TERRAFORM TEST PARENT

This host-only configuration is superseded by the one-apply workflow above.
The following evidence describes the earlier revision, not its current KMS
ownership or bootstrap filename.

Scope:
- deploy/terraform prepares one m7g.xlarge parent in an existing public subnet.
  No AWS infrastructure was created during this verification. KMS remains
  separately managed through the existing PCR-bound policy workflow.
- No signer code, image contents, KMS policy generator or launcher changed.

Commands and scenarios exercised:
- Terraform 1.16.1: fmt, init -backend=false -input=false, and validate passed
  against the signed hashicorp/aws 6.66.0 provider. The provider lock is retained.
- Four temporary mocked-provider plan scenarios passed: the restricted parent
  plan, rejection of world-open SSH (/0), rejection of a shared IPv4 subnet
  (/24), and rejection of IPv6 input. These are offline configuration checks,
  not AWS API calls, account permissions, regional availability, or live routing.
- bash -n and ShellCheck 0.11.0 passed for the final bootstrap.sh.
- Ran the final bootstrap in isolated temporary filesystems: normal completion,
  package-manager failure, and service-start failure. The failure cases stopped
  immediately without reporting readiness. Package installation, systemd,
  kernel-module and Nitro commands were simulated; shell execution and file
  writes were real. This does NOT verify actual AL2023 package installation,
  packet filtering, hugepage reservation or the Nitro device.
- Built the current pinned .#enclaver with --no-update-lock-file. readelf showed
  AArch64 with no ELF interpreter or dynamic section, and nix-store --query
  --references returned no dependencies. Copied its executable out of the store
  and ran --help and print-manifest under qemu-aarch64; both exited successfully
  and the latter loaded the real enclaver.yaml. Parent-side Nix is unnecessary
  for this pinned static runner. This is not an emulated Nitro launch.
- Temporary provider-plan fixtures, sandbox files, keys and copied executables
  were removed; no permanent mock-based test suite was added.

Still unverified: real terraform apply/destroy, EC2 cloud-init completion,
allocator capacity, Nitro boot, live KMS authorization, hardware attestation,
and restart/recovery. Use synthetic test transactions, not funds.

SOFTWARE VERIFICATION: DURABLE SAFE-WITHDRAWAL EXAMPLE

Source and artifact:
- Added examples/vault/contract.rs, predicate.rs and the runnable main.rs.
  The contract compiles locally; its fixed-recipient/capped-fee predicate runs
  as inline v1 WASM. Production service source and evaluator registry unchanged.
- guest.rs compares byte-for-byte with evaluators/common.rs from the pinned
  Sapio c8f4e63123e724dc1144075af8ac0dbd3cf4e0b1 dependency.
- The pinned Rust 1.98.1 development shell now also includes wasm32-unknown-unknown.
  build.sh --write generated the 1,317-byte artifact; --check reproduced it.
- WASM SHA256:
  1348843e73335007c919e8f0e64b897137c903fdda64244daed34611e1fee136
- Rebuilding in a separate temporary directory produced identical bytes.
  Modifying the copied artifact made --check fail without rewriting it;
  explicit --write restored the original bytes.

Actual services and client behavior:
- Built both examples and the native local-dev signer with locked dependencies.
- Ran program_oracle and vault against native and aarch64-musl local-dev
  services in separate user/network namespaces. The ARM service ran under
  qemu-aarch64, using the earlier smoke-only ARM build, not a Nitro enclave.
- All existing program_oracle scenarios passed on both services after its
  identity/CLI code was moved unchanged in behavior into examples/support/.
- The vault compiled a real Sapio contract against each service's public root,
  used the compiled funding output, and selected its program key-path request.
- 500-sat and exactly 1,000-sat fees were authorized under the same instance
  and funding. ProgramClient verified the signatures and PSBT integrity;
  Miniscript finalized and extracted transactions without changing their txids.
- Typed remote rejections covered a 1,001-sat fee, redirected recipient,
  output amount exceeding input, extra output and extra input (each retaining
  the allowed aggregate fee/recipient), unexpected witness, and substitution
  of a more permissive instance against the original funding.
- A valid sweep still signed and finalized after all rejections.
- Both clients rejected a local-dev identity without the explicit bypass and
  rejected an unexpected program profile even with that bypass.
- vault --help ran successfully. Funding/recipients were disposable fixtures;
  no transactions were broadcast or checked against a Bitcoin node.

Build checks:
- cargo check --locked --offline --all-targets --no-default-features: passed.
- cargo test --locked --offline --no-default-features: 4 passed.
- cargo test --locked --offline --features local-dev: 4 passed.
- cargo fmt --all --check: passed.
- nix flake check --no-build --no-update-lock-file: passed.
- Strict Clippy (-D warnings) fails on result_large_err in the external
  #[sapio::contract] expansion at examples/vault/contract.rs. The pinned macro
  emits a closure returning Sapio's >=192-byte CompilationError. This warning
  is not suppressed; fixing that upstream API is outside this example change.
- CI is configured to reproduce vault.wasm and run both live examples.
  No new CI run or production EIF rebuild is claimed by these local checks.

Both supervised services were stopped; temporary identities and build copies
were removed. Real Nitro boot, NSM/KMS provisioning and recovery are unverified.

SOFTWARE VERIFICATION: PROGRAM COMPATIBILITY REVIEW

ARM entrypoint correction:
- The previously built start-enclaver script selected an x86_64 Nix-store
  Bash on its first line; its second ARM shebang was only a comment.
  The pinned init launches this script with ForkExec, not an explicit shell.
- Reproduced the architecture mismatch: qemu-aarch64 rejected the selected
  Bash with "Invalid ELF image for this architecture" (exit 255).
- Replaced the script builder with writeTextFile and a single #!/bin/sh.
  /bin/sh in the image selects its static aarch64 BusyBox. The target shell
  executed under qemu-aarch64, and parsed the generated startup script with
  sh -n successfully. This does NOT exercise the full init/odyn/Nitro boot.
- nix flake check --no-build --no-update-lock-file: passed.
- nix build --no-update-lock-file .#rootfs .#eif: passed.
- Corrected local EIF: result-program-review-1/sapio_tee.eif (92,229,586 bytes).
  The older result-arm link and previously published CI artifact are NOT
  this corrected image. This local rebuild has not been reproduced in CI.
- SHA256:
  53067b229a9095e736c6f9ff16d5b6af92802a3be1c78528befa2a3ee45c7645
- Build-emitted PCR0:
  97a3b3f19158a320f5577a3bb7fe2a4a834b5b800e8a06be5808487d7d55faa05cf4c2051bdbf0b0de9ba439a27666ee
- Build-emitted PCR1 (unchanged):
  c4d45e9a07d8b1c6ebffb9afcecf900428c110a97dce1161f47e9432239c69a3714f5359cd7f7d7ab6e721af6a46a073
- Build-emitted PCR2:
  27dfd4e521b7397fc901c1b132665a1e59c8cedfe99e1132abdc7ce25a45d4e2def15da585626d23cf6f91895d90e3ac
  Independently reproduce/review these measurements before granting access;
  they are not attestation evidence or automatic replacements for trust pins.

Repeated native live signing:
- Built the local-dev binary and program_oracle example with locked dependencies.
- Started the actual HTTP/TCP service in an isolated user/network namespace.
- The example passed registered payment, authenticated script-path, inline v1
  CTV and custom inline v2 lock-time signing. It rejected wrong payment terms,
  missing script authentication, instance substitution, an unknown evaluator,
  a changed CTV template, wrong lock time, a trap and fuel exhaustion.
- A valid request still signed afterward. ProgramClient checked successful
  signatures and unrelated PSBT fields. Funding was synthetic; no broadcast.
- Stopped the supervised native service cleanly.

AArch64 runtime execution under qemu-user:
- Built a separate aarch64-musl application override adding --features local-dev.
  This smoke-only build was NOT substituted into the production EIF; the
  production build still uses --no-default-features and has no local-dev mode.
- Started that ARM executable with qemu-aarch64 in an isolated namespace.
  Its signing profile matched the native service.
- Ran the same real TCP program_oracle example against it. All acceptance,
  typed rejection, trap, fuel-exhaustion and post-rejection signing scenarios
  above passed, including cryptographic verification by ProgramClient.
- The production ARM binary's --program-profile also ran under qemu-aarch64.
- Stopped the supervised ARM service cleanly. This exercises ARM machine code
  and its WASM engine, NOT a Nitro kernel, NSM, attestation or KMS operations.

Compiled Sapio contract to the ARM service:
- A throwaway client reused the pinned upstream PaymentContract, but compiled
  it against the running ARM service's ephemeral public root instead of using
  the example's in-process oracle/private root.
- Compilation retained the same address while adding suggested candidates.
  Binding and prepare_spend produced actual ProgramClient requests.
- The ARM service signed three candidates: 5,000 and 6,000 sats at output 0,
  and 7,000 sats at output 1. ProgramClient verified the signatures; Miniscript
  finalized each PSBT; check_finalized_candidate checked its funding policy.
- Each candidate modified to pay 4,999 sats received a typed remote rejection
  under its original program commitment and funding.
- All funding was synthetic; no chain validation or broadcast was performed.
  The supervised service was stopped. The throwaway client/identity were
  removed; no new permanent test suite or CLI was added.
- The corrected production ARM executable rejected --local-dev with exit 1,
  confirming the smoke-only override was not substituted into the EIF.

Real Nitro boot, NSM/KMS provisioning and restart/recovery remain unverified.

SOFTWARE VERIFICATION: 2026-09-19 — AARCH64 EIF BUILD AND CI

Local x86_64-linux build host, cross-compiled aarch64 outputs:
- nix flake check --no-build --no-update-lock-file: passed.
- nix build --no-update-lock-file .#eif .#enclaver .#app: passed.
- readelf: sapio-tee, entrypoint, enclaver, odyn, busybox all
  "Machine: AArch64", fully static (no PT_INTERP).
- Kernel installs as Image with the ARM64 "ARM\x64" boot magic at 0x38;
  carried config has NSM, VIRTIO_MMIO, VIRTIO_VSOCKETS, VSOCKETS,
  SERIAL_8250_CONSOLE built in (=y).
- Rootfs carries /lib/ld-musl-aarch64.so.1.
- EIF emitted: sapio_tee.eif (105,884,643 bytes) plus pcr.json and log.txt.

CI (run 35451306747, ubuntu-24.04):
- Rust contract checks, attestation fixtures, and the live signing protocol
  passed on the native x86_64 toolchain.
- The aarch64 musl app, aarch64 parent runner, kernel, and measured EIF build
  passed in CI; artifact sapio-tee-aarch64-eif uploaded.
- Reproducibility: the CI EIF is bit-identical to the local build and the
  CI pcr.json matches the local one exactly (deterministic across machines).

Unexercised hardware/account boundary unchanged (see below): no aarch64
Nitro boot has been performed; PCRs above are builder measurements, not
hardware evidence.

SOFTWARE VERIFICATION: 2026-09-18 — PROGRAMORACLE 0.2.0

Rust 1.98.1, x86_64 Linux:
- cargo test --locked --no-default-features: 4 passed.
- cargo test --locked --features local-dev: 4 passed.
- cargo clippy --locked --all-targets --all-features -- -D warnings: passed.
- cargo fmt --all --check: passed.
- Final fuel-exhaustion example also rebuilt and passed targeted clippy.
- A transitive proc-macro-error2 2.0.1 dependency emits Cargo's future-Rust
  incompatibility warning; it is not a failure with the pinned compiler.

Live program services, not mock signers:
Started the native local-dev executable and a separately built musl local-dev
executable in isolated Linux user/network namespaces, leaving an unrelated
host listener on port 8000 untouched. Both used the actual app HTTP interface
and ProgramOracle TCP service on ports 8000/8367. Their signing profiles agreed
with independently generated --program-profile output.

Ran the maintained client against EACH service:
  cargo build --locked --example program_oracle
  target/debug/examples/program_oracle --address 127.0.0.1:8367 \
    --identity <that-service-identity.json> --allow-local-dev
The caller entered the corresponding network namespace with nsenter. All
funding was synthetic, with no broadcast or Bitcoin consensus-node claim.
Observed on both native and musl servers:
- The registered payment interpreter authorized two amounts/output positions
  under the SAME committed program instance and funding output.
- Key-path and authenticated script-path signing succeeded at input index 1.
- Underpayment, redirected recipient, malformed evidence, missing script-path
  authentication, instance substitution and unknown evaluator were rejected.
- Inline v1 CTV signed a matching template with native-witness prevouts and
  rejected a changed output under the original commitment/funding.
- Inline v2 enforced a committed signed-lock-time predicate, rejected a changed
  lock time, and failed closed for both a trap and an infinite fuel-consuming
  loop. These were typed remote rejections, not client transport timeouts.
- A subsequent valid payment still signed. ProgramClient cryptographically
  checked every successful signature and preservation of unrelated PSBT fields.
- Omitting --allow-local-dev caused the client to reject the local identity.

Both supervised services were stopped cleanly after verification. Temporary
identity files, smoke scripts and the separate musl-smoke symlink were removed.
Additional native service smoke:
Health returned 200; repeat setup returned 409; local attestation returned 503;
a too-short nonce returned 400. Oversized frames closed only the offending
connection; HTTP health remained ready.

Attestation verifier:
- nix develop --no-update-lock-file -c python3 -m unittest discover -s tests
  -p 'test_*.py': 10 passed.
- Real signed X509/COSE fixtures cover successful verification, payload/xpub
  substitution, wrong nonce/settings/PCRs, zero pins, stale timestamps and
  untrusted roots. Cases also reject missing signing profiles, unexpected
  registries/capabilities, wrong program identity, and malformed expected
  profiles even when authentically bound.
- CLI smoke used the production package's offline --program-profile output.
  A matching signed fixture returned its exact identity. Missing the required
  profile argument and an unexpected registry failed without printing an
  identity.
- Certificates were generated test fixtures, NOT Nitro hardware evidence.

Nix packaging and production boundary:
- nix flake check --no-build --no-update-lock-file: passed.
- nix build --no-update-lock-file .#eif --out-link result-eif: passed, including
  the production musl application and measured rootfs/EIF assembly, with the
  pinned supervisor, Go init and Linux 6.12.110.
- nix build --no-update-lock-file .#app --out-link result-app: passed.
- Packaged production --help and --program-profile ran without NSM. Default
  startup failed closed without /dev/nsm. --local-dev was rejected.
- The separate musl smoke build used an override appending --features local-dev
  to cargoBuildFlags; it was NOT substituted into the production EIF. The
  normal flake still builds production with --no-default-features.
- No parent launch, AWS resources or policy changes were performed in this
  verification.
- Local production image: result-eif/sapio_tee.eif
- EIF SHA256:
  621af08e4c978e55edda4c46f284132306c5bfafd7d05c04f7ba78d7f35971ad
- Build-emitted PCR0:
  3df4b9c0a03f57ac8138811065884c8620063334ac6fc501e4397a38bdc916285a11012b7df90bf596a9552d6bc6ca12
- Build-emitted PCR1:
  0d106c61b4afc95e0613996089036323cf03256502df0a55884ffc1d7734d9251727f47c15fb036dd040033e22ee46f3
- Build-emitted PCR2:
  bbc3fe289b4aa5cb955760d322f7876a695f16f5a2a31cffa52721dbb8f46c55b42b30ba3f66f422ec5056488fcbaad9
These are observations from this build, NOT independent trust anchors or
hardware attestation. Rebuild and compare before granting KMS access.

Unexercised hardware/account boundary:
No real Nitro boot, NSM-signed document, KMS DeriveSharedSecret call, installed
IAM-policy negative test, or restart-based KMS recovery was performed. Live
non-debug enclave verification and independent measurement reproduction
remain prerequisites for production use.
